spring:
  application:
    name: crew-service
management:
  endpoints:
    web:
      exposure:
        include: health,info
aquastream:
  logging:
    structured:
      enabled: false
    level:
      root: DEBUG
logging:
  level:
    org.aquastream.crew: DEBUG
    org.springframework.web: INFO
    root: DEBUG
---
spring:
  config:
    activate:
      on-profile: local
server:
  port: 8083
---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8083
