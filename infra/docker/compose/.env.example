# Aquastream stack environment variables (EXAMPLE)
# ---------------------------------------------------
# 📋 Versions (pinned to stable releases for small load)
# See versions.md for update schedule and reasoning

# Infrastructure services
ZOOKEEPER_TAG=3.8.4-debian-11-r0    # Bitnami stable, production ready
KAFKA_TAG=7.5.1-ccs                  # Confluent Platform compatibility
POSTGRES_TAG=16.2-alpine3.19         # Latest stable 16.x with Alpine for resource efficiency

# ELK Stack (matching versions for compatibility)
ELASTIC_TAG=8.12.2                   # Latest stable 8.x
LOGSTASH_TAG=8.12.2                  # Must match Elasticsearch
KIBANA_TAG=8.12.2                    # Must match Elasticsearch

# Monitoring stack
PROM_TAG=v2.50.1                     # Latest stable Prometheus
ALERTMANAGER_TAG=v0.27.0             # Latest stable Alertmanager
GRAFANA_TAG=10.4.1                   # LTS version for stability

# Base images for applications
NODE_TAG=20.11.1-slim               # Node.js LTS, slim for smaller size
NGINX_TAG=1.25.4-alpine             # Stable nginx with Alpine
ECLIPSE_TEMURIN_JDK_TAG=21-jdk   # Java 21 LTS for builds  
ECLIPSE_TEMURIN_JRE_TAG=21-jre   # Java 21 LTS runtime
ALPINE_TAG=3.19.1                   # Latest stable Alpine Linux

# ⚠️  SECURITY: Replace these with strong passwords! ⚠️
# Generate strong passwords: openssl rand -base64 32
POSTGRES_PASSWORD=                   # REQUIRED: Strong password for PostgreSQL
GRAFANA_ADMIN_PASSWORD=              # REQUIRED: Strong password for Grafana admin
ELASTIC_PASSWORD=                    # REQUIRED: Strong password for Elasticsearch
KIBANA_PASSWORD=                     # REQUIRED: Strong password for Kibana user

# Ports (host-facing) - через Nginx reverse proxy
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Internal ports (только для разработки, в продакшене закрыты)
KAFKA_HOST_PORT=19092
POSTGRES_PORT=5432
ELASTIC_PORT=9200
KIBANA_PORT=5601
PROM_PORT=9091
GRAFANA_PORT=3001
GATEWAY_PORT=8080
FRONTEND_PORT=3000
LOGSTASH_TCP_PORT=5000
LOGSTASH_HTTP_PORT=9600

# Logstash host/port for services
LOGSTASH_HOST=logstash
LOGSTASH_PORT=${LOGSTASH_TCP_PORT}

# 🚀 Resource limits (optimized for small load)
# CPU limits (cores)
CPU_MICRO=0.05        # Ultra-light services (placeholders)
CPU_TINY=0.15         # Microservices with minimal logic  
CPU_SMALL=0.3         # Standard services (nginx, monitoring)
CPU_MEDIUM=0.6        # Database and message broker
CPU_BIG=0.8           # Heavy services (elasticsearch)

# Memory limits 
MEM_MICRO=64M         # Ultra-light services (placeholders)
MEM_TINY=192M         # Microservices with JVM overhead
MEM_SMALL=384M        # Standard services
MEM_MEDIUM=768M       # Database with moderate cache
MEM_BIG=1536M         # Elasticsearch with modest heap

# JVM-specific memory settings (for Java services)
JVM_XMS_TINY=96M      # Initial heap for microservices
JVM_XMX_TINY=160M     # Max heap for microservices  
JVM_XMS_MEDIUM=256M   # Initial heap for gateway
JVM_XMX_MEDIUM=512M   # Max heap for gateway