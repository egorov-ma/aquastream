name: Backend CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run unit tests
        run: ./gradlew clean test

      - name: Assemble JARs
        run: ./gradlew build -x test

      - name: Upload backend build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: "**/build/libs/*.jar"

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-reports
          path: |
            **/build/reports/tests/

      # YAML syntax validation временно отключена — можно вернуть позже при добавлении конфигов.

      - name: ShellCheck infra scripts
        run: |
          find infra/scripts -type f -name "*.sh" | xargs shellcheck -e SC2164 -e SC1091

      - name: Validate docker-compose
        run: docker compose -f infra/docker/compose/docker-compose.yml config 